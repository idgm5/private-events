<h1>New event</h1>

<ul>
  <% unless @user.nil? %>
    <% @user.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  <% end %>  
</ul>

<%= render 'form', event: @event %>

<%= link_to 'Back', events_path %>