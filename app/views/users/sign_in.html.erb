<h1>Sign In</h1>

<%= form_with url: login_path  do |f| %>
        <%= f.label 'Use your name' %>
        <%= f.text_field :name %>
        <%= f.submit 'Go' %>

<% end %>

<p><%= link_to 'Back to home', root_path  %></p>