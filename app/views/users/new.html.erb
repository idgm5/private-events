<h1>Sign Up</h1>

<%= form_with model: @user do |f| %>
        <%= f.label 'Name' %>
        <%= f.text_field :name %>
        <%= f.submit %>

<% end %>

<p><%= link_to 'Back to home', root_path  %></p>
